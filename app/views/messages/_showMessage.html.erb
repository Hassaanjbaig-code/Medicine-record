<%= turbo_stream_from :message_list %>
<div id="<%= room.id %>_message" class="p-2 h-100 w-100" style="overflow-y: auto;">
  <%# <div class="messages"> %>
  <% message.each do | messages | %>
    <p><%= render partial: message, local: { message: messages } %></p>
  <% end %>